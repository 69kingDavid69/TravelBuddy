# Optional. Leave empty in production — the same FastAPI process serves the
# built React bundle from "/", so the frontend hits /chat and /tts on the same
# origin. Set this only when running the frontend against a different backend.
VITE_API_URL=
